export type albumType = {
    userId?: string
    id?: string,
    title?: string
}

export type albumImageType = {
    albumId: string,
    id: string,
    title: string,
    url: string,
    thumbnailUrl: string
}
